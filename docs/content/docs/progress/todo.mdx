---
title: TODO
description: Planned follow-up work
---

# TODO

- Complete the remaining detailed cloud-canvas manual acceptance recorded in Pending Tests. Gate 0's backend verification and native IndexedDB transactional/CAS recovery gate are complete for the user-approved Chrome and Firefox scope; the user explicitly waived macOS Safari testing, so Safari remains unverified rather than passed.
- Build the separately scoped `provider-media-input` path for external image/video Providers: each Adapter must explicitly choose bytes/base64/multipart/provider upload or a separately approved Provider-facing signed URL. A signed URL expiry remains an unapproved behavior boundary and must not inherit the browser Asset proxy decision. Yjs real-time collaboration currently exists only as architecture, with no implementation.
- Replace the Claude Code CLI adapter with a Claude Agent SDK adapter and improve the tool queue, following the [Local Agent Integration Plan](/docs/progress/local-agent-integration-plan).
- Add Skill installation from web search, Skill resource-file management, and controlled local-memory persistence.
- Complete Task 5's server-side object-storage cutover for canvas media and My Assets. Until then, stable local media keys are retained but their bytes are not portable across devices.
- Add scoped cleanup and usage visibility for orphaned canvas media and obsolete local drafts; partial deletion and cross-device deletion can otherwise leave browser storage growing until the Asset lifecycle owns reference cleanup.
- Local canvas draft garbage collection only ever runs for the scope of the canvas being opened, and only collects rows that are already synced, unreferenced, and older than six hours. Drafts belonging to canvases that are never reopened therefore keep their storage until the Asset lifecycle owns reference cleanup.
- Decide what should happen when local recovery is permanently unavailable, for example in private browsing or a storage-restricted context. Today an unavailable recovery store leaves the canvas in `recovery-blocked`, which also stops cloud saving, so the server cannot be used even though it is reachable.
- Before release, disable or remove every arbitrary-URL/user-supplied executable plugin path, including the legacy node-plugin installer, and verify that ordinary users can only select project-owner-published plugins and Provider capabilities.
