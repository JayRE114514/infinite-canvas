# Domain Docs

本项目采用单一领域上下文：存在根目录 `CONTEXT.md` 时读取与任务相关的术语，存在 `docs/adr/` 时在修改公共接口、数据所有权或一致性模型前读取相关决策。

- 文件不存在时继续工作，不为满足目录结构提前创建。
- `CONTEXT.md` 只记录稳定领域词汇，不写实现计划。
- ADR 只记录难以逆转且经过真实方案权衡的决定。
- 领域文档不能覆盖 `AGENTS.md`；发现冲突时先向用户说明。
