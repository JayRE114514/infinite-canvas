# Triage Labels

本地 ticket 使用以下状态：

| 状态 | 含义 |
| --- | --- |
| `needs-triage` | 等待维护者判断 |
| `needs-info` | 等待补充信息 |
| `ready-for-agent` | 边界明确，可由 Agent 实现 |
| `claimed` | 已由当前 Agent 接手 |
| `resolved` | 已完成 |
| `ready-for-human` | 需要人工判断或操作 |
| `wontfix` | 当前不处理 |

任何外部同步、关闭或删除仍需用户明确授权。
