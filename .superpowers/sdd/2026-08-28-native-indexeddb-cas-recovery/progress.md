# SDD ledger — plan: docs/superpowers/plans/2026-08-28-native-indexeddb-cas-recovery.md

## Identity

- Plan commit: `767035b90d60d5599b5b9aeed4c07dd3d5ba54a0`
- Authoritative spec: `docs/superpowers/specs/2026-08-26-backend-platform-architecture-design.md`
- Execution mode: sequential subagent-driven development; no parallel writers.
- Acceptance authority: Opus or Kimi only. Sol implementation self-tests are development evidence, never acceptance.
- Independent review cap: two rounds per task.

## Plan-review ruling

Plan final review cap reached after two independent rounds. The author applied the final C1/I2/M4 exact corrections without a third review. Implementation Task 1 RED/GREEN and task-level Opus/Kimi acceptance are the next executable evidence. If Task 1 or Task 6 surfaces a load-bearing plan defect, stop that task rather than layering compatibility patches. The plan is corrected and implementation-authorized; it is not recorded as independently approved.

## Preflight overlap and dependency table

| Task | Primary ownership | Shared boundary / dependency | Execution rule |
|---|---|---|---|
| 1 | `web/package.json`, `bun.lock`, Vitest config/setup, recovery `database.ts` | Produces the injected database and one-transaction runner used by all storage tasks | First; isolated harness and runner only |
| 2 | recovery `scope.ts`, `types.ts` and focused tests | Consumes only Task 1 failure type; produces scope, envelope and CAS outcome contracts | After Task 1 acceptance |
| 3 | recovery `store.ts` draft/open operations and tests | Shares the future `store.ts` with Task 4; consumes Tasks 1–2 | Task 4 must extend, not rewrite, Task 3 |
| 4 | recovery `store.ts` coordination/deletion/GC plus `bootstrap.ts` and tests | Extends Task 3 store; owns coordination and destructive storage semantics | After Task 3 acceptance |
| 5 | canvas API/repository/delete contracts and tests | Consumes existing backend `CanvasDeletionReceipt`; no storage implementation dependency | Kept sequential to preserve one-writer review history |
| 6 | sync types/session/manager and their tests | Load-bearing atomic consumer cutover; overlaps contracts introduced by Tasks 2–5 | No dual persistence protocol; stop on interface mismatch |
| 7 | legacy recovery deletion, app/consumer wiring, i18n, docs | Removes the superseded module only after Task 6 has no imports | Final integration and manual-browser handoff |

## Task status

| Task | Implementation | Independent acceptance | Commit |
|---|---|---|---|
| 1 | complete (self-tested) | APPROVE round 1/2 — Kimi, C0/I0/M0, 7/7 | `77c2fba` |
| 2 | complete (fixed/self-tested) | APPROVE final round 2/2 — Kimi, C0/I0/M0, 17/17 | `12197f0` + `33f97dd` |
| 3 | complete (fixed/self-tested) | APPROVE final round 2/2 — Opus, C0/I0/M0, 12/12 | `9a4980d` + `0b36a53` |
| 4 | complete (self-tested) | pending | recorded at acceptance |
| 5 | pending | pending | — |
| 6 | pending | pending | — |
| 7 | pending | pending | — |

## Decisions and evidence

- Existing untracked `.superpowers/research/**` belongs to the user and is excluded from every task commit.
- Web build, web typecheck, dev server and browser automation remain user-owned. Focused Node Vitest commands in the plan are required and must be run by the Opus/Kimi implementation or acceptance agent assigned to that task.
- Task 1: implementation complete and self-tested only; see `.superpowers/sdd/2026-08-28-native-indexeddb-cas-recovery/task-1-report.md`. RED was observed as an unresolved `./database` import with the suite already collected by the service-scoped include glob; GREEN is 7/7 on `vitest run src/services/canvas-recovery/database.test.ts`. Both required fake-green mutations were executed and restored: removing the deadline `transaction.abort()` let the finite request loop commit and turned the rollback assertion red, and removing `db.close()` from `onversionchange` turned the upgrade test red through its `onblocked` branch. Only the restored aborting version is committed.
- Task 1 environment note: `bun` is absent from `PATH`; the install used the already-staged `/tmp/ic-bun-1313/bun-linux-x64/bun`, version `1.3.13`, matching the repo's pinned `packageManager`. A reviewer reproducing the lockfile needs the same binary available.
- Task 1 open observation for acceptance: `web/tsconfig.json` `include` covers `vite.config.ts` and `src/**` only, so `web/vitest.config.ts` and `web/test/setup-indexeddb.ts` sit outside the typecheck program. No tsconfig change was made because the brief does not request one.
- Task 1: independent Kimi acceptance round 1/2 APPROVE, C0/I0/M0. The reviewer independently reran `cd web && ./node_modules/.bin/vitest run src/services/canvas-recovery/database.test.ts` with 7/7 passing, re-proved late-commit and blocked-upgrade detection using isolated mutants, and verified package/lockfile integrity. See `task-1-review-round-1.md`. No second round is needed.
- Task 2: implementation complete and self-tested only; acceptance pending. See `.superpowers/sdd/2026-08-28-native-indexeddb-cas-recovery/task-2-report.md`. RED was observed twice as unresolved `./scope` and `./types` imports with each suite already collected by the service-scoped include glob; GREEN is 5/5 on `vitest run src/services/canvas-recovery/scope.test.ts` and 17/17 on `vitest run src/services/canvas-recovery` (database 7, scope 5, types 5). Only these two scoped commands were run.
- Task 2 fake-green: removing the `value.scopeId !== scopeId` comparison from `asDraftRecord` turned only the "rejects a record whose stored scope differs" assertion red (1 failed | 16 passed), proving that assertion is the load-bearing cross-scope isolation guard. The comparison was restored and the suite returned to 17/17; only the restored version is committed.
- Task 2 open observations for acceptance: the draft and marker validators return the validated input by cast, so unknown extra keys survive validation; `assets` entry values and `snapshot` internals are not structurally validated; `isIsoDate` uses `Date.parse` rather than a strict ISO-8601 gate. All three follow the brief's specified validator code and are flagged for Task 3 consumers rather than changed here.
- Task 2 independent Opus acceptance round 1/2: NOT APPROVED, C2/I3/M2, despite 17/17 baseline tests. Accepted corrections: validate the full `assets` entry union; reject every non-JSON or cyclic value recursively in `snapshot` rather than only fixing its root prototype; require finite viewport coordinates and positive scale; make `readInstallationId` return `string | null`, validate before persistence and contain storage exceptions; require canonical UTC timestamps; reject marker holes/duplicate draft IDs; reject unknown scope discriminants. Rejected as non-blocking: stripping unknown record fields adds no safety, and `draftId` separator/length policy remains an internal-generation invariant for Task 3 rather than a Task 2 storage-boundary rule.
- Ruling: Task 2 plan text may be corrected in the fix commit because its `readInstallationId(): string` contract and permissive validator samples conflict with the authoritative corruption-rejection requirement. Preserve the public type/output shapes otherwise and keep the test-file count stable by extending existing cases. Cost if wrong: Task 7 could receive `null` and must handle it as controlled-unavailable; Task 3 must stop if its store assumes looser records.
- Task 2 fix round 1: implementation fixed and self-tested; final independent acceptance round 2/2 pending. The existing scope/types files remain 5+5 test cases. RED after extending those cases was 4 failed / 13 passed: unknown discriminant, invalid generated installation id, noncanonical tombstone and duplicate marker were the first exposed failures. The principled boundary now rejects the full non-JSON/cyclic snapshot graph, malformed asset entries, unusable viewport transforms, noncanonical timestamps, marker holes/duplicates and unknown scope kinds; installation reads/writes degrade to `null`. Unknown record properties remain unstripped and no `draftId` policy was added.
- Task 2 fix-round GREEN: `cd web && ./node_modules/.bin/vitest run src/services/canvas-recovery` passed 17/17 after implementation and after both tracked mutations were restored. Weakening `isJsonObject(snapshot)` to root-only `isRecord(snapshot)` failed at the nested `Date` assertion (1 failed / 16 passed). Removing `getItem` exception containment failed with `Error: get failed` (1 failed / 16 passed). See the appended Fix round 1 section in `task-2-report.md`. No build, typecheck, dev server, browser automation or broader suite was run.
- Task 2 plan correction: Task 2 now declares `readInstallationId(...): string | null` and its test/implementation samples carry the strict guards. Task 3+ interfaces and test counts are unchanged. Round-1 review evidence is tracked as `task-2-review-round-1.md`.
- Task 2 final independent Kimi acceptance round 2/2: APPROVE, C0/I0/M0, 17/17. All C1/C2/I1/I2/I3/M1 corrections were independently closed. The reviewer exercised 157 malformed/valid assertions, 11 targeted mutants, thousands of random DAG/cycle cases and 50k-deep acyclic input without finding a load-bearing regression. See `task-2-review-round-2.md`. Review cap reached; no third round exists.
- Task 3: implementation complete and self-tested only; acceptance pending. See `.superpowers/sdd/2026-08-28-native-indexeddb-cas-recovery/task-3-report.md`. RED was observed as an unresolved `./store` import with the suite already collected by the service-scoped include glob; GREEN is 12/12 on `vitest run src/services/canvas-recovery/store-draft.test.ts`. Only that scoped command was run: no build, typecheck, dev server, browser automation or broader suite.
- Task 3 fake-green: weakening the `stored.writeSeq >= record.writeSeq` guard to `>` failed 2 of 12 — the equal-seq assertion in "accepts an increasing writeSeq" and the two-connection race in "serialises two connections writing the same key", which collapsed to `['written','written']`. Both prove the CAS boundary is load-bearing, including the lost-update path. `>=` was restored and the suite returned to 12/12; only the restored version is committed.
- Task 3 store invariants: open reads epoch+marker+drafts in ONE readonly transaction; the draft CAS reads the epoch, refuses a tombstone or generation mismatch, and compares/swaps in ONE readwrite transaction. `coordinationRevision` is neither compared nor advanced by a draft write. A missing epoch row is the only path to generation zero; a present-but-invalid epoch or marker is fail-closed `corrupt`. Corrupt draft rows are skipped on read, but a corrupt row at a write target returns `unavailable: corrupt` and is preserved intact. Enumeration is exact scope-limited `index(by_scope).getAll(scopeId)` with no ambient `indexedDB` or `IDBKeyRange`.
- Ruling: Task 3 plan/brief samples were corrected in place rather than patched downstream, per the controller's first-principles requirement. (1) `upsertDraft` now validates the constructed record through Task 2's `asDraftRecord` BEFORE opening any transaction and stores that validated object unmutated, so the store can never persist a row a later open would skip; the sampled version built an unvalidated record inline. (2) Snapshot ordering compares instants via `Date.parse` with a `draftId` tiebreak, because Task 2's canonical validator admits `toISOString`'s expanded-year form and `"+275760-09-13T00:00:00.000Z"` sorts below `"1970-..."`, so the sampled `savedAt.localeCompare` ranked the chronologically newest draft last. Public types, outcome shapes and the 12-case count are unchanged. Cost if wrong: Task 4 must extend a store whose write path rejects invalid input before its transaction, and must keep instant-based ordering rather than string ordering.
- Task 3 input-validation evidence: the existing "never overwrites a corrupt existing draft" case was extended (no new `it`) to cover fractional/negative/`NaN` `writeSeq`, unparsable and noncanonical `savedAt`, an empty envelope, an unusable viewport, an unknown `state` and an empty `draftId`; after all nine rejections the scope-limited `getAll` still contains only the pre-existing corrupt row, proving no invalid input reached storage.
- Task 3 open observations for acceptance: `asDraftRecord` returns the validated input by cast, so unknown extra envelope properties still survive into storage (Task 2's accepted behaviour, unchanged here); two-tab serialisation is proven with two connections from one `fake-indexeddb` factory, so real cross-tab browser behaviour remains user-owned manual verification.
- Task 3 independent Kimi acceptance round 1/2: NOT APPROVED, C0/I0/M1, 12/12. The sole finding is accepted: `draftId.localeCompare` is default-locale dependent and contradicts the corrected plan's cross-browser deterministic tiebreak claim. Replace it with explicit UTF-16 code-unit comparison and pin a mixed-case equal-timestamp pair inside the existing ordering test. The reviewer independently verified 40/40 equal-sequence races, unequal-sequence winner correctness, pre-aborted zero-write behavior, invalid-input zero-transaction behavior, expanded-year instant ordering, exact-scope isolation and corrupt-row preservation; no other correction is authorized for this fix pass.
- Task 3 fix round 1: M1 fixed and self-tested; final independent acceptance round 2/2 pending. `readScopeDrafts` now uses explicit UTF-16 code-unit comparison for equal-`savedAt` draft IDs instead of default-locale `localeCompare`. The existing expanded-year/ordering case remains one of 12 and now pins `"A"` before `"a"` at an identical timestamp.
- Task 3 fix-round RED/GREEN: with production still using `localeCompare`, the extended mixed-case assertion failed exactly as intended — received `["d3","d1","a","A","d2"]` versus code-unit `["d3","d1","A","a","d2"]` (1 failed / 11 passed). After the one-line comparator fix, `cd web && ./node_modules/.bin/vitest run src/services/canvas-recovery/store-draft.test.ts` passed 12/12.
- Task 3 fix-round mutation: restoring `a.draftId.localeCompare(b.draftId)` turned the same mixed-case assertion red with 1 failed / 11 passed. The explicit `<` / `>` comparator was restored and the focused suite returned to 12/12. Only the restored locale-independent version is committed. No build, typecheck, dev server, browser automation or broader suite was run.
- Task 3 final independent Opus acceptance round 2/2: APPROVE, C0/I0/M0, 12/12. M1 is closed; the explicit code-unit comparator and mixed-case assertion remained green under en/sv/tr/C environments while a `localeCompare` relapse failed under all four. Transaction, validation and CAS paths were unchanged and control mutants still failed. See `task-3-review-round-2.md`. Review cap reached; no third round exists.
- Ruling: Task 4 owns the shared `store.ts` extension and must close two cross-task preflight defects without reopening Task 3 review. (1) Caller-owned envelope/marker/keep arrays can be mutated after synchronous validation but before an IndexedDB request resumes; capture a detached structured clone before the first await so no validated object can change underneath the transaction. Cover both draft upsert and marker coordination. (2) confirmed deletion must enumerate and delete every marker key in the scope, including a corrupt non-`conflict` marker ID, rather than deleting only the canonical key. Task 4 acceptance owns these regressions. Cost if wrong: a caller race could persist a record the read validator rejects, or confirmed deletion could leave durable corrupt marker residue.
- Ruling: every Task 4 epoch increment must refuse safe-integer overflow, and non-tombstoned `deletionGeneration` mismatch is corrupt state rather than `already-tombstoned`. Bootstrap storage exceptions return `failed`; they do not escape the declared outcome union. These are storage-boundary invariants, not compatibility branches, and the project has no released legacy data.
- Task 4: implementation complete and self-tested only; acceptance pending. See `.superpowers/sdd/2026-08-28-native-indexeddb-cas-recovery/task-4-report.md`. RED was observed twice: all 11 coordination cases failing on `store.commitCoordination is not a function` / `createLazyBrowserRecoveryStore is not a function`, and the bootstrap suite failing on an unresolved `./bootstrap` import, each already collected by the service-scoped include glob. GREEN is 11/11 coordination, 3/3 bootstrap and 43/43 on `vitest run src/services/canvas-recovery` (database 7, scope 5, types 5, store-draft 12, store-coordination 11, bootstrap 3). Only those three scoped commands were run: no build, typecheck, dev server, browser automation or non-recovery test.
- Task 4 store invariants: all three operations share ONE readwrite transaction over epochs+markers+drafts, ordered validate-detached-input → read epoch → refuse tombstone → compare both expected values → validate the next epoch → validate every delete target → first write. `commitCoordination` and `collectGarbage` advance only `coordinationRevision` and never write a tombstone or touch `deletionGeneration`; `confirmDeletion` is the sole generation-advancing operation and always writes the tombstone in the same transaction. Task 3's `upsertDraft` keeps its narrower epochs+drafts scope and still ignores `coordinationRevision`.
- Task 4 fake-green: six mutants, each reverted, all failing the intended assertion. Required pair — skipping `confirmDeletion`'s draft deletion left two rows against an empty-drafts expectation (1 failed / 10 passed), and moving the marker write before corrupt delete-target validation lost the marker despite an unavailable outcome (1 failed / 10 passed). Plus: undetached marker input (2 failed / 9 passed), unvalidated epoch spread returning `committed` at the safe-integer ceiling, canonical-marker-key-only deletion leaving a noncanonical marker row, and undetached draft envelope losing the original envelope. The focused suite returned to 11/11 after each revert; only restored versions are committed.
- Task 4 caller-mutation and overflow proof: both required regressions start the operation without awaiting, mutate the caller's object, await a microtask so the mutation lands while requests are pending, mutate again, then await. Stored marker stays `[entry("d1")]` with the delete set unwidened, the stored envelope equals the original, and an emptied-then-repopulated keep list changes neither GC's keeps nor its collections. Uncloneable envelopes/marker entries return `unavailable/corrupt` instead of throwing DataCloneError. At a `MAX_SAFE_INTEGER` epoch all three operations refuse, and invalid revisions/generations/`now`/`minAgeMs`/keep IDs are refused before any transaction opens; the epoch row and the scope's drafts are re-read afterwards and are byte-for-byte unchanged, proving no partial write.
- Task 4 confirmed-deletion boundary: a present tombstone alone returns `already-tombstoned` regardless of the expected generation and deletes nothing; a non-tombstoned generation mismatch is unreachable under valid state and fails closed `unavailable/corrupt` without deleting data; on a match both drafts and markers are enumerated by `SCOPE_INDEX` and every key is deleted, including corrupt draft rows and a marker parked under a noncanonical `markerId` (a seeded `"legacy-residue"` row is asserted gone).
- Task 4 bootstrap: import is side-effect free and the export surface is pinned to exactly `["upgradeRecoveryStorage"]` with no legacy read path. An unreadable `getItem` returns `failed` without calling `dropLegacy`; a throwing drop returns `failed` unrecorded; a successful drop whose `setItem` throws also returns `failed` and stays retryable, so `dropLegacy` MUST be idempotent — documented in the function and asserted by a two-run case. The lazy browser adapter reads `globalThis.indexedDB` only on first use, returns `unavailable/unsupported` for all five operations without it, and works normally with an injected factory.
- Ruling: Task 4 corrected its own plan/brief samples in place per the controller's binding rulings, keeping public outcome unions and the planned 11 + 3 case counts stable by extending existing `it` cases; no compatibility branch was invented and no contract conflict arose. One change fell outside the planned file list: Task 2's module-private count predicate is now exported from `types.ts` as `isRecoveryCount`, because Ruling 5 requires reusing existing validators as the single source for safe nonnegative revisions/generations. No validation logic was altered. Cost if wrong: Task 5+ consumers must treat `unavailable/corrupt` as the response to unsafe epoch state and invalid destructive input, and must supply an idempotent `dropLegacy`.
- Task 4 open observations for acceptance: two-tab serialisation is still proven with two connections from one `fake-indexeddb` factory, so real cross-tab behaviour remains user-owned manual verification; `detach` relies on `structuredClone` (Node 17+, no polyfill added); GC's unparsable-`savedAt` keep branch is unreachable through the validator and defensive only; `browserCanvasRecoveryStore` stays exported but unconsumed until Task 6/7; tombstone rows are retained indefinitely with no epoch compaction.
